[package]
name = "enzato"
version = "1.0.0"
edition = "2021"


build = "build.rs"

[dependencies]
crossterm = "0.28.1"

[build-dependencies]

winres = "0.1.12"

[profile.release]
opt-level = 3          
lto = true             
codegen-units = 1      
panic = "unwind"       
strip = false          